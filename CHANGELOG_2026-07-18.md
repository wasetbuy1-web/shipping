# سجل التعديلات — 2026-07-18

تنفيذ الميزات الست المتفق عليها. **لم يتم لمس أي شيء متعلق بالجوال/Responsive/viewport** — تصميم سطح المكتب كما هو 100%.

الملف الوحيد المعدَّل: `index.html`. ملف جديد واحد: `sql/007_create_product_weights.sql`.

> إذا ما عجبك أي بند، قله لي بالاسم (مثلاً "رجّع البند 4") وأرجعه زي ما كان — كل بند مستقل عن الباقي.

---

## 1) FedEx Economy كخيار شحن غير مباشر

**أُضيف:**
- سطر جديد في `CARRIERS`: `{ id: 'fedex_economy', label: 'MyUS FedEx Economy', carrierCode: 'FEDEX_ECONOMY', ... }` — بجانب DHL و FedEx Priority تمامًا بنفس الآلية (يُحمَّل تلقائيًا من `SHIPPING_PAIRS_TO_PRELOAD` بدون أي تعديل إضافي).
- إضافة `'fedex_economy'` إلى كل القوائم السبع التي كانت تحصر الخصم والرسوم الإضافية على `['fedex', 'dhl']` فقط، حتى يُعامَل بنفس الأسلوب تمامًا.

**بخصوص وقت التوصيل:** لم أضع أي قيمة افتراضية/مخترعة — تركت `time: ''`. الواجهة أصلاً تُفضّل البيانات الحقيقية القادمة من `shipping_rates` (`delivery_days`) ولا تعرض `time` إلا إذا كانت تلك البيانات غير متوفرة لوزن معيّن — هذا نفس السلوك المستخدم مع بقية الشركات، لا شيء خاص أُضيف لـ FedEx Economy.

**لم يُحذف شيء هنا.**

---

## 2) تبديل شركة الشحن غير المباشر (DHL / FedEx Priority / FedEx Economy)

اكتشفت أثناء التنفيذ أن المشكلة كانت في **مكانين**، وليس مكانًا واحدًا فقط:

1. `getProductSummary()` — كانت تُعيد ضبط `p.carrierId` على الأرخص دائمًا عند كل حساب.
2. دالة عرض قائمة شركات الشحن (بجانب أزرار الاختيار نفسها) — كانت **أيضًا** تُعيد ضبط `p.carrierId` على الأرخص عند كل رندر.

كلا الموضعين كانا يُلغيان اختيار المستخدم فور الضغط على أي شركة غير الأرخص. **عدّلت الاثنين معًا** بحيث:
- إذا كان اختيار المستخدم الحالي ما زال متاحًا وله سعر — يُحترَم ولا يُستبدل.
- إذا لم يكن هناك اختيار بعد، أو أصبح الاختيار السابق غير متاح (محظور/بدون سعر) — يرجع تلقائيًا للأرخص، تمامًا كالسلوك القديم.

النتيجة: يمكن الآن الضغط على أي شركة شحن غير مباشر والبقاء عليها، مع تحديث فوري للسعر الإجمالي ورسالة واتساب (الأرقام فقط تتغير — لا شيء في نص الرسالة أو أسماء الشركات تغيّر).

**لم يُحذف شيء — فقط أُصلح سلوك خاطئ.**

---

## 3) أزرار لصق من الحافظة (Clipboard)

**أُضيف:**
- زر أيقونة لصق صغير بجانب حقول **رابط المنتج / اسم المنتج / سعر المنتج** فقط، في مكانين:
  - النموذج الرئيسي لكل منتج.
  - جدول "منتج من نفس الموقع" المختصر.
- يستخدم `navigator.clipboard.readText()` مباشرة بدون أي نافذة تأكيد أو واجهة إضافية، تمامًا كما طُلب.
- تمت إضافة الأيقونة **داخل** صندوق الحقل نفسه (بدون تحريك أي حقل آخر) — أضفت مسافة داخلية صغيرة يمينًا داخل هذه الحقول الثلاثة فقط لإفساح مكان للأيقونة (لم يتغير أي شيء في بقية الحقول أو باقي الصفحة).

**لم يُحذف شيء.**

---

## 4) حفظ وزن المنتج → قاعدة البيانات (Supabase)

هذا أكبر تعديل، ويتضمن تغييرات وحذوفات حقيقية يجب الانتباه لها:

### ما تم تنفيذه
- **جدول جديد** `product_weights` في Supabase (ملف `sql/007_create_product_weights.sql`) — يحتوي فقط: `product_url`, `product_name`, `weight_lb` (+ id وتواريخ). **يجب تشغيل هذا الملف يدويًا في Supabase SQL editor — لم أُنفّذه أنا، فقط كتبته.**
- زر "حفظ الوزن": يبحث أولًا عن سجل بنفس الرابط + الاسم (مطابقة غير حساسة لحالة الأحرف/المسافات) — إن وُجد يُحدَّث (`UPDATE`)، وإلا يُنشأ سجل جديد (`INSERT`). لا يوجد تكرار أبدًا.
- المطابقة التلقائية (عرض الوزن المحفوظ أثناء الكتابة) أصبحت تشترط تطابق **الرابط والاسم معًا**، بدل الاسم فقط سابقًا.
- عرض الروابط الطويلة أصبح مختصرًا (اسم النطاق فقط، مثل `amazon.com`) مع الاحتفاظ بالرابط الكامل في الـ href — نفس الأسلوب المطلوب في الكانبان.

### ما تم حذفه (مهم)
- **حقل "الوحدة" (كجم/رطل) وحقل "مصدر الوزن" حُذفا من سجل الوزن المحفوظ** — الوزن يُخزَّن بالرطل دائمًا الآن (تحويل تلقائي عند الحفظ إن كان المدخل بالكجم). هذا مطابق لطلبك الأصلي ("Weight (LB) فقط").
- **الأوزان المحفوظة القديمة (المخزّنة محليًا في المتصفح/localStorage) لن تُهاجَر تلقائيًا لقاعدة البيانات** — الميزة الآن تقرأ فقط من Supabase. إذا كان عندك أوزان محفوظة سابقًا في المتصفح ومهمة لك، قلي قبل ما تُفقد نهائيًا وأقدر أساعدك تصدّرها أولًا (لم أحذف أي بيانات فعليًا من الجهاز، فقط توقف الكود عن قراءتها/كتابتها إلى localStorage).
- ملف تصدير Excel: أُزيل عمودا "الوحدة" و"مصدر الوزن"، وأُضيف عمود "رابط المنتج" (صار مطلوبًا). **ملفات Excel المُصدَّرة بالتنسيق القديم لن تُستورَد بنجاح بعد الآن** لأنها لا تحتوي على عمود الرابط.

---

## 5) إصلاح زر "تعديل" في الكانبان

سطر واحد فقط: كان يُمرّر إلى أعلى الصفحة كاملة (`#ordersPage`) بدل نموذج التعديل الفعلي. أصبح يُمرّر مباشرة إلى `.orderFormPanel` — نفس الطريقة المستخدمة أصلًا في زر آخر بالصفحة.

**لم يُحذف شيء.**

---

## 6) روابط المنتجات داخل بطاقة الكانبان

**أُضيف:** قسم جديد يعرض كل روابط منتجات الطلب، مباشرة تحت "آخر تحديث" وقبل باقي تفاصيل البطاقة (بما فيها قائمة الحالة المنسدلة) — تمامًا كما طُلب. الروابط تُعرض مختصرة (اسم النطاق) مع الاحتفاظ بالرابط الكامل عند الضغط عليها (تفتح في تبويب جديد).

**لم يتغيّر أو يُحذف أي شيء آخر في البطاقة** — فقط إضافة قسم جديد، بدون تحريك أي عنصر موجود.

---

## 7) زر "فتح واتساب" بجانب "نسخ الرسالة" (أُضيف بعد سؤالك)

**أُضيف:** زر جديد بجانب "نسخ الرسالة" في صفحة الحاسبة. عند الضغط عليه يفتح واتساب مباشرة (تطبيق أو ويب) مع نص الرسالة جاهزًا بالكامل — **بدون رقم محدد**، حتى تختار أنت الشخص اللي تبي ترسل له من قائمة جهات الاتصال عندك. هذا يجاوب بالضبط على سؤالك: "يفتح الواتس عندي وأرسل للي أبغاه".

- لم يُحذف أو يتغيّر زر "نسخ الرسالة" — بقي كما هو، هذا زر إضافي بجانبه فقط.
- نص الرسالة نفسه لم يتغيّر إطلاقًا — نفس النص المستخدم في النسخ تمامًا.
- لا علاقة له بزر "فتح واتساب" الموجود في صفحة إدارة الطلبات (ذاك يفتح رابط رقم محفوظ لكل طلب، وبقي كما هو بدون أي تعديل).

---

## ملفات تم تغييرها

| الملف | نوع التغيير |
|---|---|
| `index.html` | تعديل (كل البنود أعلاه) |
| `sql/007_create_product_weights.sql` | **جديد** — يجب تشغيله يدويًا في Supabase |

## لم يُمس إطلاقًا
- أي CSS أو تخطيط خاص بسطح المكتب.
- الـ viewport، أو أي شيء متعلق بالجوال/الاستجابة (Responsive).
- منطق حساب الشحن الأساسي، هيكلة قاعدة البيانات الحالية (غير الجدول الجديد)، تدفق الطلبات، أو نص رسالة واتساب (غير رقم الشحن نفسه).

---

## قائمة اختبار الدخان (Smoke Test) — بعد تشغيل SQL

نفّذها بالترتيب على نسخة حقيقية من الموقع (مو محليًا فقط)، وافتح Console (F12) قبل ما تبدأ عشان تلاحظ أي خطأ أحمر يطلع أثناء التجربة.

1. **تحميل الصفحة بدون أخطاء** — افتح الموقع، تأكد ما فيه أي خطأ أحمر في Console، وتأكد إعدادات → "الأوزان محفوظة في قاعدة البيانات: N وزن محفوظ" تظهر رقم (حتى لو صفر) بدون رسالة فشل تحميل.
2. **FedEx Economy يظهر كخيار** — أنشئ منتج بشحن "غير مباشر" وأدخل وزن، وتأكد "MyUS FedEx Economy" يظهر في قائمة شركات الشحن بجانب DHL و FedEx Priority، وله سعر حقيقي (مو فاضي أو صفر).
3. **الخصم يشتغل على FedEx Economy** — غيّر نسبة "خصم FedEx و DHL" وتأكد سعر FedEx Economy يتغيّر معهم مثل البقية بالضبط.
4. **اختيار الشركة يثبت** — اضغط على FedEx Economy (أو أي شركة غير الأرخص)، تأكد صار "نشط"، وتأكد **السعر الإجمالي** ورسالة واتساب تحدّثوا فورًا لنفس السعر.
5. **الاختيار ما يرجع لوحده** — بعد الخطوة السابقة، غيّر شي بسيط بالمنتج (مثلاً الكمية)، وتأكد الشركة المختارة **ما ترجع للأرخص تلقائيًا** — لازم تبقى نفس الشركة اللي اخترتها.
6. **رسالة واتساب ما تغيّر نصها** — قارن نص رسالة واتساب قبل وبعد تبديل الشركة: لازم يتغيّر رقم الشحن فقط، بدون أي تغيير بالكلام أو اسم الشركة داخل الرسالة.
7. **أزرار اللصق تشتغل** — انسخ أي نص، ثم اضغط أيقونة اللصق بجانب "رابط المنتج" و"اسم المنتج" و"سعر المنتج" كل وحد لحاله، وتأكد النص يترصف بالحقل الصحيح فورًا بدون أي نافذة تأكيد.
8. **حفظ الوزن لأول مرة** — أدخل رابط + اسم + وزن لمنتج، اضغط "حفظ الوزن"، وتأكد يطلع توست "تم حفظ الوزن" بدون خطأ.
9. **الاسترجاع التلقائي يطابق الاثنين معًا** — امسح الوزن من نفس المنتج (بدون تغيير الرابط أو الاسم) ثم اكتبهم من جديد يدويًا، وتأكد صندوق "سجل الأوزان المحفوظة" يطلع تلقائيًا بنفس الوزن المحفوظ.
10. **ما يطابق إذا اختلف أحدهما** — غيّر الرابط فقط (خلّي الاسم زي ما هو) لمنتج جديد، وتأكد **ما يطلع** أي وزن محفوظ — لازم الاثنين يتطابقوا مع بعض.
11. **التحديث يستبدل ولا يكرر** — احفظ وزن مختلف لنفس الرابط + الاسم اللي حفظته بالخطوة 8 (رقم وزن جديد)، اضغط "حفظ الوزن"، وتأكد يطلع توست "تم تحديث الوزن المحفوظ" (مو "تم حفظ" كأنه جديد) — وتأكد بقائمة "الأوزان المحفوظة" بالإعدادات إنه صف واحد بس مو صفين.
12. **تصدير/استيراد Excel** — من الإعدادات، صدّر ملف الأوزان وتأكد يحتوي عمود "رابط المنتج"، ثم جرّب استيراده مرة ثانية وتأكد ما يتكرر أي صف.
13. **زر "تعديل" بالكانبان** — روح "إدارة الطلبات"، اضغط "تعديل" على أي طلب، وتأكد الصفحة تنزل مباشرة لنموذج التعديل نفسه (مو لأعلى الصفحة أو لوحة الكانبان).
14. **روابط المنتجات بالبطاقة** — تأكد كل بطاقة طلب فيها منتج تعرض روابط المنتجات مباشرة تحت "آخر تحديث" وقبل باقي التفاصيل، بشكل مختصر (اسم النطاق)، وتأكد الضغط عليها يفتح الرابط الكامل الصحيح بتبويب جديد.
15. **زر "فتح واتساب"** — بصفحة الحاسبة، اضغط "فتح واتساب" بجانب "نسخ الرسالة"، وتأكد واتساب يفتح والرسالة جاهزة بدون رقم محدد (تختار الشخص بنفسك)، وتأكد "نسخ الرسالة" لسا يشتغل زي ما كان.

إذا كل بند نجح → النسخة جاهزة للاعتماد. إذا أي بند فشل، قلي رقمه وأرجع لك ذاك الجزء فقط.
